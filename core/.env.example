# Copy to .env (gitignored) and fill in. Only needed for live API calls
# (@pytest.mark.api tests and the router/eval paths in later splits).
# No-key unit tests run green without this set.
ANTHROPIC_API_KEY=sk-ant-...
